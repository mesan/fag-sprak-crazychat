require "spec_helper"

describe Crazychat do
  it "has a version number" do
    expect(Crazychat::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
